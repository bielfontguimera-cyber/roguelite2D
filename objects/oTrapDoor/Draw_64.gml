var gw = display_get_gui_width();
var gh = display_get_gui_height();

var cx = gw * 0.5;
var cy = 150;


if (player_overlapping = true) {
    draw_set_color(c_white);
    draw_set_halign(fa_center);
    draw_set_valign(fa_middle);
    draw_set_alpha(0.8);
    draw_set_font(fnt_3)
    draw_text(cx, cy, "Press E to go to the next floor"); 
    } else exit;